# Backlog Items

Store local backlog items in this directory using the schema defined in `docs/backlog/schema.md`.

Recommended filename format:

- `BL-0001-short-slug.md`

Use `docs/backlog/templates/item.md` as the starting point.
